# Graph Updater Module

Applies node/edge updates to graph files on structure changes.

## Guidelines
1. If a file is added/removed, edit `graph/nodes.json` and `graph/graph-index.json`.
2. Map imports/exports or component relationships.
3. Keep the file syntax valid JSON.
